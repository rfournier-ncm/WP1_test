---
title: "wrangle"
author: "Raph & Rich"
date: "`r Sys.Date()`"
output: html_document
editor_options: 
  chunk_output_type: console
---

This file wrangles data for Exp 4 in the face familiarity study.

```{r}
#| label: setup
#| include: false

pkgs <- c(
  "tidyverse", "here", "patchwork"
  )
vapply(
  pkgs, library, logical(1), character.only = TRUE, logical.return = TRUE
  )

theme_set(
  theme_bw() +
    theme(
      text  = element_text(size = 15, face = "bold"),
      title = element_text(size = 15, face = "bold"),
      legend.position = "none"
    )
)

safe_pal   <- paletteer::paletteer_d("rcartocolor::Safe", 4)

myColors_2 <- c("Novel" = safe_pal[1], "Famous" = safe_pal[3])

pd2        <- position_dodge(1)
```

# 1. Read in raw data

Raw data files are not stored in the repo. This section reads them in and
saves a combined file. Skip to section 2 if the combined file already exists.

```{r}
#| label: read-raw
#| eval: false

raw <- list.files(
  path       = here("exp_4", "data", "raw"),
  pattern    = "*csv",
  full.names = TRUE,
  recursive  = TRUE
) |>
  set_names() |>
  map(read_csv, 
      col_types = cols(.default = "c"),
      show_col_types = FALSE
      ) |>
  list_rbind(names_to = "file")

write_csv(raw, here("exp_4", "data", "raw_exp_4.csv"))
```

# 2. Wrangle

```{r}
#| label: read-combined

raw <- read_csv(here("exp_4", "data", "raw_exp_4.csv"))
```

```{r}
#| label: wrangle

data <- raw |>
  filter(!is.na(recognition_trial_number)) |>
  rename(
    pid      = participant,
    keypress = key_resp_recognition.keys,
    cond     = identity_type,
    rt       = key_resp_recognition.rt
  ) |>
  mutate(
    block = rep(1:3, each = 60, times = n_distinct(pid)),
    cond = case_when(
      cond == "familiar" ~ "Famous",
      cond == "novel" ~ "Novel"
    ),
    cond = factor(cond, levels = c("Famous", "Novel")),
    keypress = as.factor(keypress),
    acc = case_when(
      keypress == "q" & cond == "Famous" ~ 1L,
      keypress == "p" & cond == "Novel" ~ 1L,
      TRUE ~ 0L
    ),
    block = as.integer(block)
  ) |>
  select(pid, block, cond, keypress, acc, rt) |>
  arrange(pid)

write_csv(data, here("exp_4", "data", "wrangle_exp_4.csv"))
```

# 3. Exclusions

```{r}
#| label: exclusions

# familiarity ratings exclusion first
exclude_ratings <- raw |>
  filter(!is.na(familiarity_rating)) |>
  mutate(
    pid                = participant,
    familiarity_rating = as.numeric(familiarity_rating)
  ) |>
  group_by(pid) |>
  summarise(n_fam = sum(familiarity_rating > 6)) |>
  filter(n_fam < 30) |>
  pull(pid)

# RT exclusion on remaining participants
exclude_rt <- data |>
  filter(!pid %in% exclude_ratings) |>
  group_by(pid) |>
  summarise(perc_excluded = mean(is.na(rt)) * 100) |>
  filter(perc_excluded > 25) |>
  pull(pid)

# accuracy exclusion on remaining participants
exclude_acc <- data |>
  filter(!pid %in% exclude_ratings, !pid %in% exclude_rt) |>
  drop_na(rt) |>
  group_by(pid) |>
  summarise(perc_acc = mean(acc) * 100) |>
  filter(perc_acc < 55 | perc_acc > 95) |>
  pull(pid)

# apply all exclusions and remove missed trials
data <- data |>
  filter(
    !pid %in% exclude_ratings,
    !pid %in% exclude_rt,
    !pid %in% exclude_acc,
    !is.na(rt),
    rt <= 1.1,  # enforced live in PsychoPy/Pavlovia
    rt >= 0.2   # preregistered lower bound 
  )
```

```{r}
#| label: data-check

data |>
  group_by(pid, block, cond) |>
  summarise(n = n(), .groups = "drop")

data |>
  group_by(pid, block, cond) |>
  summarise(acc = mean(acc), .groups = "drop")

cat("Ratings exclusions:", length(exclude_ratings), "\n")
cat("RT exclusions:",      length(exclude_rt),      "\n")
cat("Accuracy exclusions:", length(exclude_acc),    "\n")
cat("Final n:",            n_distinct(data$pid),    "\n")
```

# 4. Summary statistics

```{r}
#| label: summary-rt-acc

rt_pid <- data |>
  group_by(pid, block, cond) |>
  summarise(
    mean = mean(rt),
    sd   = sd(rt),
    n    = n(),
    sem  = sd / sqrt(n),
    .groups = "drop"
  )

rt_group <- data |>
  group_by(cond) |>
  summarise(
    mean = mean(rt),
    sd   = sd(rt),
    n    = length(unique(pid)),
    sem  = sd / sqrt(n),
    .groups = "drop"
  )

acc_pid <- data |>
  group_by(pid, block, cond) |>
  summarise(
    sum  = sum(acc),
    n    = n(),
    perc = sum / n * 100,
    .groups = "drop"
  )

acc_group <- acc_pid |>
  group_by(cond) |>
  summarise(
    mean_perc = mean(perc),
    sd        = sd(perc),
    n         = n(),
    sem       = sd / sqrt(n),
    .groups = "drop"
  )
```

# 5. Plots

```{r}
#| label: plot-rt-bar

p_rt <- ggplot(rt_group, aes(x = cond, y = mean, fill = cond, colour = cond)) +
  geom_col() +
  geom_errorbar(
    aes(ymin = mean - sem, ymax = mean + sem),
    colour = "black", width = 0.2
  ) +
  geom_point(
    data     = rt_pid |> group_by(pid, cond) |> summarise(mean = mean(mean), .groups = "drop"),
    aes(y    = mean),
    colour   = "black",
    alpha    = 0.7,
    position = pd2
  ) +
  scale_fill_manual(values   = myColors_2) +
  scale_colour_manual(values = myColors_2) +
  coord_cartesian(ylim = c(0, 2)) +
  labs(x = "Condition", y = "Mean RT [s]")

p_rt

```

```{r}
#| label: plot-acc-bar

p_acc <- ggplot(acc_group, aes(x = cond, y = mean_perc, fill = cond, colour = cond)) +
  geom_col() +
  geom_errorbar(
    aes(ymin = mean_perc - sem, ymax = mean_perc + sem),
    colour = "black", width = 0.2
  ) +
  geom_point(
    data     = acc_pid |> group_by(pid, cond) |> summarise(mean_perc = mean(perc), .groups = "drop"),
    aes(y    = mean_perc),
    colour   = "black",
    alpha    = 0.7,
    position = pd2
  ) +
  scale_fill_manual(values   = myColors_2) +
  scale_colour_manual(values = myColors_2) +
  coord_cartesian(ylim = c(0, 100)) +
  labs(x = "Condition", y = "Accuracy [%]")

p_acc

```

```{r}
#| label: plot-rt-density

p_rt_density <- ggplot(data, aes(x = rt, fill = cond, colour = cond)) +
  geom_density(alpha = 0.7) +
  scale_fill_manual(values   = myColors_2) +
  scale_colour_manual(values = myColors_2) +
  labs(x = "RT [s]")

p_rt_density

```

```{r}
#| label: combined-plots

(p_rt | p_acc) / p_rt_density

if (!dir.exists(here("exp_4", "figures"))) {
  dir.create(here("exp_4", "figures"), recursive = TRUE)
}

ggsave(here("exp_4", "figures", "rt_acc.jpeg"), width = 8, height = 6)
```

# 6. Demographics 

```{r}
#| label: demographics

demo_exp4 <- read_csv(here("exp_4", "data", "demo_exp_4.csv")) |>
  filter(
    Status != "RETURNED",
    `Participant id` %in% unique(data$pid)
  ) |>
  mutate(
    pid    = as.character(`Participant id`),
    age    = as.numeric(Age),
    gender = as.character(Sex)
  ) |>
  select(pid, age, gender)

```

# 7. Save

```{r}
#| label: save

# save data to model
write_csv(data, here("exp_4", "data", "data_exp_4.csv"))

# save exclusion counts as a small csv
exclusions <- tibble(
  started         = 58L,
  withdrew        = 12L,
  completed       = 46L,
  exclude_rt      = length(exclude_rt),
  exclude_acc     = length(exclude_acc),
  over_95         = NA_integer_,
  under_55        = NA_integer_,
  exclude_ratings = length(exclude_ratings),
  final_n         = n_distinct(data$pid)
)
write_csv(exclusions, here("exp_4", "data", "exclusions_exp_4.csv"))

# save cleaned demographics as a small csv
write_csv(demo_exp4, here("exp_4", "data", "demo_exp_4_clean.csv"))
```
